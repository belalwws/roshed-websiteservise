'use client';import {useEffect} from 'react';import {usePathname} from 'next/navigation';
export default function Reveal(){const path=usePathname();useEffect(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches||!('IntersectionObserver' in window))return;const elements=[...document.querySelectorAll('.section-heading,.program-card,.benefit,.about-art,.family-portrait,.about-photo,.value-card,.story')];const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');observer.unobserve(e.target)}}),{threshold:.08});elements.forEach((el,i)=>{el.classList.add('reveal');el.style.setProperty('--delay',(i%3)*65+'ms');observer.observe(el)});return()=>{observer.disconnect();elements.forEach(el=>el.classList.remove('reveal','revealed'))}},[path]);return null;}

